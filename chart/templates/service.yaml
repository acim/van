apiVersion: v1
kind: Service
metadata:
  name: {{ include "van.fullname" . }}
  labels:
    {{- include "van.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "van.selectorLabels" . | nindent 4 }}
